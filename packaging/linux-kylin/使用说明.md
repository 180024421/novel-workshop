# 大帅墨枢 · 麒麟 / Linux

1. 下载 `DashuaiMoshu-*-linux-*.AppImage`
2. `chmod +x DashuaiMoshu-*.AppImage`
3. 双击或终端运行

或安装 deb：

```bash
sudo dpkg -i DashuaiMoshu-*-linux-*.deb
```

打开大帅墨枢 → 设置 → 粘贴 API Key 即可写作，无需外置网关。
